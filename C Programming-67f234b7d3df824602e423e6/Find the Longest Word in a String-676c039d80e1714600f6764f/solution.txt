[{"code":"#include <stdio.h>\r\n#include <string.h>\r\n#include <ctype.h>\r\n\r\nvoid findLongestWord(const char *str) {\r\n    int maxLen = 0, currLen = 0;\r\n    int maxStart = 0, currStart = 0;\r\n    int i = 0;\r\n\r\n    while (str[i] != '\\0') {\r\n        if (!isspace(str[i])) {\r\n            if (currLen == 0) {\r\n                currStart = i;\r\n            }\r\n            currLen++;\r\n        } else {\r\n            if (currLen > maxLen) {\r\n                maxLen = currLen;\r\n                maxStart = currStart;\r\n            }\r\n            currLen = 0;\r\n        }\r\n        i++;\r\n    }\r\n\r\n    // Check last word\r\n    if (currLen > maxLen) {\r\n        maxLen = currLen;\r\n        maxStart = currStart;\r\n    }\r\n\r\n    \r\n    for (i = 0; i < maxLen; i++) {\r\n        putchar(str[maxStart + i]);\r\n    }\r\n   \r\n}\r\n\r\nint main() {\r\n    char str[100];\r\n\r\n      fgets(str, sizeof(str), stdin);\r\n\r\n    // Remove trailing newline character if present\r\n    str[strcspn(str, \"\\n\")] = '\\0';\r\n\r\n    findLongestWord(str);\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-14T11:40:27.383Z","isCorrect":true}]