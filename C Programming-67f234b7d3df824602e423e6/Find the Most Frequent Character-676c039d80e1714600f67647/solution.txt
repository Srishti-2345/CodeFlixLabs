[{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\nint main() {\r\n    char str[1000];\r\n    int count[256] = {0};  // Array to store frequency of each character\r\n    int i, maxCount = 0;\r\n    char maxChar;\r\n\r\n    \r\n    fgets(str, sizeof(str), stdin);\r\n\r\n    // Remove the newline character if present\r\n    size_t len = strlen(str);\r\n    if (len > 0 && str[len - 1] == '\\n') {\r\n        str[len - 1] = '\\0';\r\n    }\r\n\r\n    // Count the frequency of each character\r\n    for (i = 0; str[i] != '\\0'; i++) {\r\n        count[(unsigned char)str[i]]++;\r\n    }\r\n\r\n    // Find the character with the maximum frequency\r\n    for (i = 0; i < 256; i++) {\r\n        if (count[i] > maxCount) {\r\n            maxCount = count[i];\r\n            maxChar = (char)i;\r\n        }\r\n    }\r\n\r\n    if (maxCount > 0) {\r\n        printf(\"%c\", maxChar);\r\n    }\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-14T11:04:12.344Z","isCorrect":true},{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\nint main() {\r\n    char str[1001]; // Assuming max length of input string is 1000\r\n    int freq[256] = {0}; // Frequency array for all ASCII characters\r\n    int i, max_freq = 0;\r\n    char result;\r\n\r\n    // Read the input string (including spaces if any)\r\n    scanf(\"%[^\\n]\", str);\r\n\r\n    // Count the frequency of each character\r\n    for (i = 0; str[i] != '\\0'; i++) {\r\n        freq[(unsigned char)str[i]]++;\r\n    }\r\n\r\n    // Find the most frequent character (smallest ASCII in case of tie)\r\n    for (i = 0; i < 256; i++) {\r\n        if (freq[i] > max_freq) {\r\n            max_freq = freq[i];\r\n            result = i;\r\n        }\r\n    }\r\n\r\n    printf(\"%c\\n\", result);\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-28T18:36:28.864Z","isCorrect":true}]