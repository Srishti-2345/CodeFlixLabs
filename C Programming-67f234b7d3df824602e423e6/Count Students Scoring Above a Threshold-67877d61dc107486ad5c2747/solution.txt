[{"code":"#include <stdio.h>\r\n\r\n// Define the structure\r\nstruct Student {\r\n    int rollNumber;\r\n    char name[50];\r\n    float marks;\r\n};\r\n\r\nint main() {\r\n    int n, count = 0;\r\n    float threshold;\r\n\r\n    // Read number of students\r\n    scanf(\"%d\", &n);\r\n    struct Student students[n];\r\n\r\n    // Read student data\r\n    for (int i = 0; i < n; i++) {\r\n        scanf(\"%d %s %f\", &students[i].rollNumber, students[i].name, &students[i].marks);\r\n    }\r\n\r\n    // Read the threshold value\r\n    scanf(\"%f\", &threshold);\r\n\r\n    // Count students scoring above the threshold\r\n    for (int i = 0; i < n; i++) {\r\n        if (students[i].marks > threshold) {\r\n            count++;\r\n        }\r\n    }\r\n\r\n    // Output the count\r\n    printf(\"%d\\n\", count);\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-27T17:24:03.257Z","isCorrect":false},{"code":"#include <stdio.h>\r\n\r\n// Define the structure\r\nstruct Student {\r\n    int rollNumber;\r\n    char name[50];\r\n    float marks;\r\n};\r\n\r\nint main() {\r\n    int n, count = 0;\r\n    float threshold;\r\n\r\n    // Read number of students\r\n    scanf(\"%d\", &n);\r\n    struct Student students[n];\r\n\r\n    // Read student data\r\n    for (int i = 0; i < n; i++) {\r\n        scanf(\"%d %s %f\", &students[i].rollNumber, students[i].name, &students[i].marks);\r\n    }\r\n\r\n    // Read the threshold value\r\n    scanf(\"%f\", &threshold);\r\n\r\n    // Count students scoring above the threshold\r\n    for (int i = 0; i < n; i++) {\r\n        if (students[i].marks > threshold) {\r\n            count++;\r\n        }\r\n    }\r\n\r\n    // Output the count\r\n    printf(\"Count of students scoring above %.2f: %d\\n\",threshold, count);\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-27T17:25:15.310Z","isCorrect":true}]