[{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\nvoid removeSpaces(char *str) {\r\n    int count = 0;\r\n    for (int i = 0; str[i]; i++) {\r\n        if (str[i] != ' ') {\r\n            str[count++] = str[i];\r\n        }\r\n    }\r\n    str[count] = '\\0';\r\n}\r\n\r\nint main() {\r\n    char str[100];\r\n\r\n   \r\n    fgets(str, sizeof(str), stdin);\r\n\r\n    // Remove trailing newline character if present\r\n    str[strcspn(str, \"\\n\")] = '\\0';\r\n\r\n    removeSpaces(str);\r\n    printf(\"%s\", str);\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-14T11:37:15.193Z","isCorrect":true},{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\nint main() {\r\n    char str[1000]; // Declare a character array to store the input string.\r\n                    // Assuming a maximum string length of 999 characters + null terminator.\r\n    int i, j;       // Declare loop counters.\r\n\r\n    scanf(\"%[^\\n]\",str);\r\n\r\n    \r\n    for (i = 0, j = 0; str[i] != '\\0'; i++) {\r\n        // If the current character is not a space, copy it to the 'j' position.\r\n        if (str[i] != ' ') {\r\n            str[j] = str[i];\r\n            j++; // Increment 'j' to move to the next position for the modified string.\r\n        }\r\n    }\r\n    str[j] = '\\0'; // Null-terminate the modified string at the 'j' position.\r\n\r\n    // Print the modified string to standard output.\r\n    printf(\"%s\\n\", str);\r\n\r\n    return 0; // Indicate successful execution of the program.\r\n}","language":"c","time":"2025-05-28T19:03:08.691Z","isCorrect":true}]