[{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\n#define CHAR 256\r\n\r\nchar firstNonRepeatingChar(const char *str) {\r\n    int count[CHAR] = {0};\r\n    int i;\r\n\r\n    // Count occurrences of each character\r\n    for (i = 0; str[i]; i++) {\r\n        count[(unsigned char)str[i]]++;\r\n    }\r\n\r\n    // Find the first character with count 1\r\n    for (i = 0; str[i]; i++) {\r\n        if (count[(unsigned char)str[i]] == 1) {\r\n            return str[i];\r\n        }\r\n    }\r\n\r\n    return '\\0'; // Return null character if no non-repeating character is found\r\n}\r\n\r\nint main() {\r\n    char str[100];\r\n\r\n    \r\n    fgets(str, sizeof(str), stdin);\r\n\r\n    // Remove newline character if present\r\n    size_t len = strlen(str);\r\n    if (len > 0 && str[len - 1] == '\\n') {\r\n        str[len - 1] = '\\0';\r\n    }\r\n\r\n    char result = firstNonRepeatingChar(str);\r\n\r\n    if (result) {\r\n        printf(\"%c\", result);\r\n    } else {\r\n        printf(\"No non-repeating character found.\\n\");\r\n    }\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-15T07:28:30.555Z","isCorrect":false},{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\n#define CHAR 256\r\n\r\nchar firstNonRepeatingChar(const char *str) {\r\n    int count[CHAR] = {0};\r\n    int i;\r\n\r\n    // Count occurrences of each character\r\n    for (i = 0; str[i]; i++) {\r\n        count[(unsigned char)str[i]]++;\r\n    }\r\n\r\n    // Find the first character with count 1\r\n    for (i = 0; str[i]; i++) {\r\n        if (count[(unsigned char)str[i]] == 1) {\r\n            return str[i];\r\n        }\r\n    }\r\n\r\n    return '\\0'; // Return null character if no non-repeating character is found\r\n}\r\n\r\nint main() {\r\n    char str[100];\r\n\r\n    \r\n    fgets(str, sizeof(str), stdin);\r\n\r\n    // Remove newline character if present\r\n    size_t len = strlen(str);\r\n    if (len > 0 && str[len - 1] == '\\n') {\r\n        str[len - 1] = '\\0';\r\n    }\r\n\r\n    char result = firstNonRepeatingChar(str);\r\n\r\n    if (result) {\r\n        printf(\"%c\", result);\r\n    } else {\r\n        printf(\"-\");\r\n    }\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-15T07:29:13.728Z","isCorrect":true}]