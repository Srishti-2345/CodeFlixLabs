[{"code":"#include <stdio.h> // Required for input/output functions like printf and scanf\r\n#include <string.h> // Required for string manipulation functions like strcpy and strlen\r\n\r\n// Define the structure named 'Student'\r\n// A structure is a user-defined data type that allows you to combine data items\r\n// of different kinds.\r\nstruct Student {\r\n    int roll_no;      // Field to store the student's roll number (integer)\r\n    char name[50];    // Field to store the student's name (character array/string)\r\n    float marks;      // Field to store the student's marks (floating-point number)\r\n};\r\n\r\nint main() {\r\n    int N; // Variable to store the number of students\r\n\r\n    printf(\"--- Enter Number of Students ---\\n\");\r\n    printf(\"Enter the number of students (N): \");\r\n    // Read the number of students\r\n    if (scanf(\"%d\", &N) != 1 || N <= 0) {\r\n        printf(\"Invalid input for N. Please enter a positive integer.\\n\");\r\n        return 1; // Exit with an error code\r\n    }\r\n\r\n    // Clear the input buffer after reading N\r\n    while (getchar() != '\\n');\r\n\r\n    // Declare an array of 'struct Student' to store details for N students\r\n    // This creates an array of Student structure instances.\r\n    struct Student students[N];\r\n\r\n    printf(\"\\n--- Enter Details for %d Students ---\\n\", N);\r\n\r\n    // Loop N times to get input for each student\r\n    for (int i = 0; i < N; i++) {\r\n        printf(\"\\nEnter details for Student %d:\\n\", i + 1);\r\n\r\n        // Get input for Roll Number\r\n        printf(\"Enter Roll Number: \");\r\n        if (scanf(\"%d\", &students[i].roll_no) != 1) {\r\n            printf(\"Invalid input for Roll Number. Please enter an integer.\\n\");\r\n            return 1; // Exit with an error code\r\n        }\r\n\r\n        // Clear the input buffer after reading the integer\r\n        while (getchar() != '\\n');\r\n\r\n        // Get input for Name\r\n        printf(\"Enter Name: \");\r\n        // fgets reads a line from stdin into students[i].name.\r\n        if (fgets(students[i].name, sizeof(students[i].name), stdin) == NULL) {\r\n            printf(\"Error reading name.\\n\");\r\n            return 1; // Exit with an error code\r\n        }\r\n        // Remove the trailing newline character that fgets might add\r\n        students[i].name[strcspn(students[i].name, \"\\n\")] = 0;\r\n\r\n        // Get input for Marks\r\n        printf(\"Enter Marks: \");\r\n        if (scanf(\"%f\", &students[i].marks) != 1) {\r\n            printf(\"Invalid input for Marks. Please enter a number.\\n\");\r\n            return 1; // Exit with an error code\r\n        }\r\n    }\r\n\r\n    printf(\"\\n--- Displaying Student Details ---\\n\");\r\n\r\n    // Loop N times to display the details of each student\r\n    for (int i = 0; i < N; i++) {\r\n        printf(\"Roll Number: %d, Name: %s, Marks: %.2f\\n\",\r\n               students[i].roll_no, students[i].name, students[i].marks);\r\n    }\r\n\r\n    return 0; // Indicate successful execution\r\n}\r\n","language":"c","time":"2025-05-27T17:13:10.444Z","isCorrect":false},{"code":"#include <stdio.h> // Required for input/output functions like printf and scanf\r\n#include <string.h> // Required for string manipulation functions like strcpy and strlen\r\n\r\n// Define the structure named 'Student'\r\n// A structure is a user-defined data type that allows you to combine data items\r\n// of different kinds.\r\nstruct Student {\r\n    int roll_no;      // Field to store the student's roll number (integer)\r\n    char name[50];    // Field to store the student's name (character array/string)\r\n    float marks;      // Field to store the student's marks (floating-point number)\r\n};\r\n\r\nint main() {\r\n    int N; // Variable to store the number of students\r\n\r\n    \r\n    // Read the number of students\r\n    if (scanf(\"%d\", &N) != 1 || N <= 0) {\r\n        printf(\"Invalid input for N. Please enter a positive integer.\\n\");\r\n        return 1; // Exit with an error code\r\n    }\r\n\r\n    // Clear the input buffer after reading N\r\n    while (getchar() != '\\n');\r\n\r\n    // Declare an array of 'struct Student' to store details for N students\r\n    // This creates an array of Student structure instances.\r\n    struct Student students[N];\r\n\r\n    \r\n\r\n    // Loop N times to get input for each student\r\n    for (int i = 0; i < N; i++) {\r\n        \r\n        // Get input for Roll Number\r\n        \r\n        if (scanf(\"%d\", &students[i].roll_no) != 1) {\r\n            printf(\"Invalid input for Roll Number. Please enter an integer.\\n\");\r\n            return 1; // Exit with an error code\r\n        }\r\n\r\n        // Clear the input buffer after reading the integer\r\n        while (getchar() != '\\n');\r\n\r\n        // Get input for Name\r\n       \r\n        // fgets reads a line from stdin into students[i].name.\r\n        \r\n        \r\n        // Remove the trailing newline character that fgets might add\r\n        students[i].name[strcspn(students[i].name, \"\\n\")] = 0;\r\n\r\n        // Get input for Marks\r\n        \r\n        if (scanf(\"%f\", &students[i].marks) != 1) {\r\n            printf(\"Invalid input for Marks. Please enter a number.\\n\");\r\n            return 1; // Exit with an error code\r\n        }\r\n    }\r\n\r\n    \r\n\r\n    // Loop N times to display the details of each student\r\n    for (int i = 0; i < N; i++) {\r\n        printf(\"Roll Number: %d, Name: %s, Marks: %.2f\\n\",\r\n               students[i].roll_no, students[i].name, students[i].marks);\r\n    }\r\n\r\n    return 0; // Indicate successful execution\r\n}\r\n","language":"c","time":"2025-05-27T17:14:33.959Z","isCorrect":false},{"code":"#include <stdio.h>\r\n\r\n// Define the structure for Student\r\nstruct Student {\r\n    int rollNumber;\r\n    char name[50];\r\n    float marks;\r\n};\r\n\r\nint main() {\r\n    int n;\r\n\r\n    // Read the number of students\r\n    scanf(\"%d\", &n);\r\n\r\n    struct Student students[n];\r\n\r\n    // Input details for each student\r\n    for(int i = 0; i < n; i++) {\r\n        scanf(\"%d %s %f\", &students[i].rollNumber, students[i].name, &students[i].marks);\r\n    }\r\n\r\n    // Output student details\r\n    for(int i = 0; i < n; i++) {\r\n        printf(\"Roll Number: %d, Name: %s, Marks: %.1f\\n\",\r\n               students[i].rollNumber, students[i].name, students[i].marks);\r\n    }\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-27T17:17:47.901Z","isCorrect":false}]