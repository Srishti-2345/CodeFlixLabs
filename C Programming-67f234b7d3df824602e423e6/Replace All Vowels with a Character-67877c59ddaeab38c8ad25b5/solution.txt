[{"code":"#include <stdio.h>\r\n#include <string.h>\r\n#include <ctype.h>\r\n\r\n// Function to replace vowels in a string with a specified character\r\nvoid replaceVowels(char str[], char replaceChar) {\r\n    for (int i = 0; str[i] != '\\0'; i++) {\r\n        // Check if the character is a vowel (both uppercase and lowercase)\r\n        if (strchr(\"aeiouAEIOU\", str[i])) {\r\n            str[i] = replaceChar; // Replace vowel with specified character\r\n        }\r\n    }\r\n}\r\n\r\nint main() {\r\n    char str[100], replaceChar;\r\n\r\n    // Input the string\r\n    \r\n    fgets(str, sizeof(str), stdin);\r\n    str[strcspn(str, \"\\n\")] = '\\0'; // Remove newline character if present\r\n\r\n    // Input the character to replace vowels with\r\n    \r\n    scanf(\"%c\", &replaceChar);\r\n\r\n    // Call the function to replace vowels\r\n    replaceVowels(str, replaceChar);\r\n\r\n    // Output the modified string\r\n    printf(\"%s\", str);\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-15T08:23:34.775Z","isCorrect":true}]