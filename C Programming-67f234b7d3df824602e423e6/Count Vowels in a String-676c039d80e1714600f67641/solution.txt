[{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\nint main() {\r\n    char str[1000];\r\n    int i, vowels = 0;\r\n\r\n\r\n    fgets(str, sizeof(str), stdin);\r\n\r\n    // Iterate through each character of the string\r\n    for (i = 0; str[i] != '\\0'; i++) {\r\n        // Check for both lowercase and uppercase vowels\r\n        if (str[i] == 'a' || str[i] == 'e' || str[i] == 'i' ||\r\n            str[i] == 'o' || str[i] == 'u' || str[i] == 'A' ||\r\n            str[i] == 'E' || str[i] == 'I' || str[i] == 'O' ||\r\n            str[i] == 'U') {\r\n            vowels++;\r\n        }\r\n    }\r\n\r\n    printf(\"%d\", vowels);\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-14T10:45:51.760Z","isCorrect":true},{"code":"#include <stdio.h>  // Required for input/output functions (printf, fgets)\r\n#include <string.h> // Required for string manipulation functions (strlen, strcspn)\r\n#include <ctype.h>  // Required for character handling functions (tolower)\r\n\r\n\r\nint isVowel(char c) {\r\n    c = tolower(c); // Convert character to lowercase\r\n    return (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u');\r\n}\r\n\r\nint main() {\r\n    char str[100]; // Declare a character array (string) to store the input.\r\n                   // 100 is chosen as a reasonable maximum length for the string.\r\n    int count = 0; // Variable to store the count of vowels\r\n    int i;         // Loop counter\r\n\r\n    \r\n    scanf(\"%[^\\n]s,str\");\r\n    \r\n    \r\n\r\n    \r\n    for (i = 0; str[i] != '\\0'; i++) {\r\n        // Check if the current character is a vowel using the isVowel helper function\r\n        if (isVowel(str[i])) {\r\n            count++; \r\n        }\r\n    }\r\n\r\n    \r\n    // Display the total number of vowels found\r\n    printf(\"%d\", count);\r\n\r\n    return 0; // Indicate successful execution\r\n}\r\n","language":"c","time":"2025-05-28T16:21:38.900Z","isCorrect":false}]