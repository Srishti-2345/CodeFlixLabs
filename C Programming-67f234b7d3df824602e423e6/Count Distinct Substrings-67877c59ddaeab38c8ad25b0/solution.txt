[{"code":"#include <stdio.h>\r\n#include <string.h>\r\n#include <stdlib.h>\r\n\r\n// Check if substring exists in array\r\nint isAlreadyPresent(char substrings[][1000], int count, char *substr) {\r\n    for (int i = 0; i < count; i++) {\r\n        if (strcmp(substrings[i], substr) == 0) {\r\n            return 1; // Found duplicate\r\n        }\r\n    }\r\n    return 0; // Not found\r\n}\r\n\r\nint main() {\r\n    char str[1000];\r\n    \r\n    fgets(str, sizeof(str), stdin);\r\n\r\n    int length = strlen(str);\r\n    if (str[length - 1] == '\\n') {\r\n        str[length - 1] = '\\0';  // Remove newline\r\n        length--;\r\n    }\r\n\r\n    char substrings[500000][1000]; // Large array to store substrings\r\n    int count = 0;\r\n\r\n    for (int i = 0; i < length; i++) {\r\n        for (int j = i; j < length; j++) {\r\n            char temp[1000];\r\n            int k, idx = 0;\r\n            for (k = i; k <= j; k++) {\r\n                temp[idx++] = str[k];\r\n            }\r\n            temp[idx] = '\\0';\r\n\r\n            if (!isAlreadyPresent(substrings, count, temp)) {\r\n                strcpy(substrings[count], temp);\r\n                count++;\r\n            }\r\n        }\r\n    }\r\n\r\n    printf(\"%d\", count);\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-15T07:57:07.636Z","isCorrect":true}]