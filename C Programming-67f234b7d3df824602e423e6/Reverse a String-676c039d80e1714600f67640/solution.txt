[{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\nint main() {\r\n    char ch[1000];\r\n    \r\n    fgets(ch, sizeof(ch), stdin);\r\n\r\n\r\n    size_t len = strlen(ch);\r\n    for (int i = len - 1; i >= 0; i--) {\r\n        putchar(ch[i]);\r\n    }\r\n    printf(\"\\n\");\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-14T10:38:16.212Z","isCorrect":false},{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\nint main() {\r\n    char ch[1000];\r\n    \r\n    fgets(ch, sizeof(ch), stdin);\r\n\r\n\r\n    size_t len = strlen(ch);\r\n    for (int i = len - 1; i >= 0; i--) {\r\n        putchar(ch[i]);\r\n    }\r\n    \r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-14T10:38:45.330Z","isCorrect":false},{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\nint main() {\r\n    char ch[1000];\r\n        fgets(ch, sizeof(ch), stdin);\r\n\r\n       size_t len = strlen(ch);\r\n    if (len > 0 && ch[len - 1] == '\\n') {\r\n        ch[len - 1] = '\\0';\r\n        len--;\r\n    }\r\n    for (int i = len - 1; i >= 0; i--) {\r\n        putchar(ch[i]);\r\n    }\r\n    \r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-14T10:40:50.293Z","isCorrect":true},{"code":"#include <stdio.h>  // Required for input/output functions (printf, fgets)\r\n#include <string.h> // Required for string manipulation functions (strlen, strcspn)\r\n\r\nint main() {\r\n    char str[100]; // Declare a character array (string) to store the input.\r\n                   // 100 is chosen as a reasonable maximum length for the string.\r\n    int length;    // To store the length of the string\r\n    int i, j;      // Loop counters\r\n    char temp;     // Temporary variable for swapping characters\r\n\r\n    \r\n\r\n   \r\n    scanf(\"%[^\\n]s\",str);\r\n       \r\n\r\n    \r\n    str[strcspn(str, \"\\n\")] = '\\0';\r\n\r\n    \r\n    length = strlen(str);\r\n\r\n    \r\n    for (i = 0, j = length - 1; i < j; i++, j--) {\r\n        temp = str[i];    // Store the character at the beginning\r\n        str[i] = str[j];  // Replace the beginning character with the end character\r\n        str[j] = temp;    // Replace the end character with the stored beginning character\r\n    }\r\n\r\n    \r\n    // Display the reversed string\r\n    printf(\"%s\", str);\r\n\r\n    return 0; // Indicate successful execution\r\n}\r\n","language":"c","time":"2025-05-28T16:13:53.366Z","isCorrect":true}]